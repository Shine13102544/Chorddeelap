// หมายเหตุ: ไฟล์นี้ไม่ได้ถูกโหลดโดย index.html (ไม่มี <script src="script.js">)
// ฟังก์ชันที่เคยอยู่ที่นี่ (setRhythmMode ฯลฯ) ถูกย้ายไปรวมอยู่ใน <script> ท้าย index.html แล้ว
// เพื่อไม่ให้โค้ด logic กระจายอยู่คนละไฟล์กันจนหลุด sync แนะนำให้แก้ไข logic ทั้งหมดที่ index.html แทน
